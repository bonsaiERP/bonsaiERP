- content_for :links do
  .span6 &nbsp;
  .links.span6
    =link_to "Nuevo proyecto", new_project_path, :class => 'new'


%h1 Proyectos


=paginate (@projects)

%table.table.table-striped
  %thead
    %tr
      %th Nombre
      %th Activo
      %th Fecha de inicio
      %th Fecha de finalización
      %th
  
  %tbody
    - for project in @projects
      %tr{ :class => cycle(:odd, :even) }
        %td= link_to(project.name, project, :class => 'b')
        %td{:class => project.active.to_s }
        %td= lo(project.date_start)
        %td= lo(project.date_end)
        %td.actions= bonsai_links(project)
