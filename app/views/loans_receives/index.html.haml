%h1 Prestamos recibidos

%ul.listing.unstyled
  - @loans.each do |loan|
    %li.row-fluid
      .span6
        %h4.ib
          = link_to loan, loan

        = link_to loan.contact, loan.contact, title: 'Contacto'
      .span6
        .moneybox
          %label TOTAL
          %h4
            = ntc loan.total
            = currency_label loan.currency
        .moneybox
          %label SALDO
          %h4
            = ntc loan.amount
            = currency_label loan.currency
