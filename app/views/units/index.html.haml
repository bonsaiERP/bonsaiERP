- content_for :links do
  %ul.breadcrumbs
    %li
      Unidades de medida

.row-fluid
  .span6
    %h1 Unidades de medida
  .span6.r
    =link_to 'Nueva unidad', new_unit_path, class: "btn"

%table.table.table-striped
  %tr
    %th Nombre
    %th Símbolo
    %th

  - @units.each do |unit|
    %tr
      %td
        %strong= unit
      %td
        = unit.symbol
      %td
        = link_to icon_edit, edit_unit_path(unit), class: 'dark'
        = link_to icon_delete, unit, class: 'dark', method: :destroy, confirm: 'Esta segur@ de borrar la Unidad de medida?'
