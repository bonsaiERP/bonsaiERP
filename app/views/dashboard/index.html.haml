- content_for :links do
  %ul.breadcrumbs.span6
    %li
      %span Resumen

  .span6
    %ul.breadcrumbs.r
      %a#general-aspects-link.dark(href="javascript:;")
        %i.icon-question-sign
          Ayuda

= render file: "dashboard/tour/general"

%h1.title Resumen

= render file: 'dashboard/general_aspects'

= @dashboard.pendent_ledgers
.row-fluid
  .span6
    %h4.n Egresos por ítem en los últimos 30 días
    %ul.unstyled
      = render partial: 'items/expenses', collection: @dashboard.expenses_by_item, as: :item

  .span6
    %h4.n Ingresos por ítem en los últimos 30 días
    %ul.unstyled
      = render partial: 'items/expenses', collection: @dashboard.incomes_by_item, as: :item

-#= render 'dashboard/demo_colors'
