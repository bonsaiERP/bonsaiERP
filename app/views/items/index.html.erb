<h1>Items</h1>

<%= link_to "Nuevo", new_item_path, :class => "new" %>


  <table class="decorated">
    <tr>
      <th>Nombre</th>
      <th>Unidad</th>
      <th>Descripción</th>
      <th>Producto</th>
      <th>Stockable</th>
      <th></th>
    </tr>

  <% @items.each do |item| %>
    <tr>
      <td><%= link_to item.name, item %></td>
      <td><%= item.unit.name %></td>
      <td><%= item.description %></td>
      <td><%= bonsai? item.product %></td>
      <td><%= bonsai? item.stockable %></td>
      <td>
        <%= link_to "editar", edit_item_path(item), :class => 'edit_icon' %> 
        <%= link_to "borrar", item, :class => 'delete_icon', :remote => true %> 
      </td>
    </tr>
  <% end %>
  </table>

