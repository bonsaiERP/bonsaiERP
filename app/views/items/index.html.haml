%h1 Items, productos y Servicios

%ul{:class => 'menu'}
  %li
    %a{ :href => 'javascript:', :class => 'new' } 
      Nuevo <span class="more"></span>
    %ul
      %li
        = link_to "Nuevo ítem", new_item_path(:ctype => Item::TYPES[0])
      %li
        = link_to "Nuevo producto", new_item_path(:ctype => Item::TYPES[1])
      %li
        = link_to "Nuevo servicio", new_item_path(:ctype => Item::TYPES[2])


%table{ :class=> "decorated mt10" }
  %tr
    %th Nombre
    %th Código
    %th Unidad
    %th Descripción
    %th Producto
    %th Stockable
    %th

  - @items.each do |item|
    %tr
      %td= link_to item.name, item
      %td= item.code 
      %td= item.unit.name 
      %td= item.description 
      %td{ :class => item.product }
      %td{ :class => item.stockable }
      %td= bonsai_links(item)
