%h1 Recuperación de contraseña en <span style="font-style: italic">bonsai</span>

%p 
  Si usted a solicitado la recuperación de la contraseña por favor
  =link_to "haga click es en este vínculo", url_for(:controller => :reset_passwords, :action => :edit, :id => @user.id, :reset_password_token => @user.reset_password_token, :host => @host), :style => 'font-weight:bold'
  de lo contrario elimine este email.

