- content_for :links do
  .span6 &nbsp;
  .links.span6= link_to "Nueva caja", new_cash_path, :class => 'new'

.page-header
  %h1 Cuentas caja

- if @cashes.empty?
  %h3= link_to "Crear nueva caja", new_cash_path, :class => 'add'

%ul#cashes.unstyled
  - @cashes.each do |cash|
    %li
      %h3=link_to cash, cash

  -# @cashes.each do |cash|
    %li
      ._55.fl
        %h3= link_to cash, cash
        %h4.mt5.dark= cash.currency_name.pluralize
      ._45.fl
        .moneybox.fl
          %label TOTAL EN CUENTA
          %h3= with_currency cash.account, :amount

        = pendent_money_link(cash, :class => 'b fs110')          
      .clear

