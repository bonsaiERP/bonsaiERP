- content_for :links do
  %ul.breadcrumps
    %li #{link_to "Cuentas caja", cashes_path}<span>&gt;</span>
  .links
    = link_to 'Nueva', new_cash_path, :class => 'new'
    = link_to 'Editar', edit_cash_path(@cash), :class => 'edit'


%h1.mb5 <span class="dark_grey n">Caja:</span> #{@cash.name}
%h3.mt5.dark_grey= @cash.currency

%hr/

.moneybox.fl
  %label Saldo en caja
  %h3= with_currency(@account, :amount)

//- if @cash.pendent_account_ledgers.any?
//  .moneybox.fl
//    %label Saldo a conciliar
//    %h3= with_currency @cash 
//
._40.fr
  %label Dirección
  = nl2br(@cash.address)
.clear

%h2 Transacciones
= render "account_ledgers/money", :account => @cash.account, :ledgers => @ledgers, :ref_title => 'Referencia'

.clear
