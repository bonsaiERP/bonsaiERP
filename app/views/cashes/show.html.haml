- content_for :links do
  %ul.breadcrumps.span6
    %li #{link_to "Cuentas caja", cashes_path}
  .links.span6
    = link_to 'Editar', edit_cash_path(@cash), :class => 'edit'

.page-header
  %h1.n
    = @cash.name
    %small= @account.currency_code

= pendent_money_link(@cash, :class => 'red fs110')
.clear

.moneybox.fl
  %label SALDO EN CAJA
  %h3= with_currency(@account, :amount)

._40.fr
  %label Dirección
  = nl2br(@cash.address)
.clear

.page-header
  %h2 Transacciones

  %ul
    - @account.account_ledgers.each do |al|
      %li
        =al
        =al.amount
        =al.attributes

-#= render "account_ledgers/money", :account => @account, :ledgers => @ledgers, :paged_ledgers => @paged_ledgers, :ref_title => 'Referencia', :money => @cash

.clear
