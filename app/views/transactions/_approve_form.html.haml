= simple_form_for @transaction, :url => "/#{params[:controller]}/#{ @transaction.id }/approve", :html => {:id => 'approve_form'} do |f|
  .submit
    =f.submit "Aprobar", :class => 'btn btn-primary', :id => 'income_approve_button'
:javascript
  $(function() {
    $('#approve_form').submit(function() {
        if(confirm("Esta seguro de aprobar la proforma (no podrá editar en un futuro)?")) {
          return true;
        }else{
          return false;
        }
    });
  })
