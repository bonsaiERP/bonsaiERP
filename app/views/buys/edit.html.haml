- content_for :links do
  %ul.breadcrumps
    %li #{link_to 'Compras y gastos', buys_path} 
  .links
    = link_to 'Ver', @transaction, :class => 'show'

%h1 Editar compra/gasto

= render 'transactions/form', :transaction => @transaction, :contacts => Supplier.scoped
