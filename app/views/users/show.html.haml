- content_for :links do
  %ul.breadcrumbs.span6
    %li Datos personales


.row-fluid
  .span6
    %h1= current_user
    %span.muted Token:
    = current_user.tenant_link(tenant).api_token
  .span6.r
    .btn-group
      = link_to edit_user_path(current_user), class: 'btn' do
        %i.icon-user
        Editar datos perfil

      = link_to new_user_password_path, class: 'btn' do
        %i.icon-key
        Cambiar contraseña

<br/><br/>
.row-fluid
  .span3
    %span.muted Nombres:
    = current_user.first_name
  .span3
    %span.muted Apellidos:
    = current_user.last_name

  .span3
    %i.icon-phone.muted(title="Teléfono" data-toggle="tooltip")
    = current_user.phone
  .span3
    %i.icon-mobile.muted(title="Móvil" data-toggle="tooltip")
    = current_user.mobile

.row-fluid
  .span6
    %i.icon-envelope.muted(title="E-mail" data-toggle="tooltip")
    = mail_to current_user.email unless current_user.email.blank?
  .span6
    %i.icon-building.muted(title="Dirección" data-toggle="tooltip")
    = nl2br current_user.address
