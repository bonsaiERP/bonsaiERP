- content_for :links do
  %ul.breadcrumbs.span6
    %li
      =link_to 'Contactos', contacts_path


.row-fluid
  .span6
    %h2 Contactos
  .span6.r
    = link_to icon_new_text, new_contact_path, class: 'btn'

.row-fluid
  .span6
    = render 'contacts/search'

- paginate_html = paginate(@contacts)

= paginate_html

%ul.unstyled.listing
  = render @contacts

= paginate_html

:javascript
  $(function() {
    $('.contact').each(function() {
      cb = new App.ContactBalance(this)
      cb.render()
    })
  })
