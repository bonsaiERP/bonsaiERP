%li.row-fluid(id="#{income.id}")
  .span4.income-detail
    .fl
      %input.row-check(type='checkbox' id="#{income.id}" code-name="#{ income }" currency="#{ income.currency }")
    .fl
      %h4.ib
        = link_to income do
          %span.code(title="#{t("general.code")}")= income
          = income.has_error_label

      = link_to income.contact, income.contact, title: t("general.contact")

      .clearfix
      %span.status= income.state_tag
      %i= l income.date
      = income.tax_tag
      = income.inventory_tag

      %tagsfor(tag_ids="#{income.tag_ids}" id="#{income.id}")

  .span4
    = income.updater_label
    = income.creator_label
    = income.approver_label
    = income.nuller_label

    = income.due_date_tag

    .clear-both
    %i(class='icon-file muted' title='Descripción')
    = income.description

  .span3
    .span6.r
      %span.visible-phone.ib.muted Total
      = ntc income.total
      = currency_label income.currency

    .span6.r
      %span.visible-phone.ib.muted Saldo
      = ntc income.balance
      = currency_label income.currency
  .span1.r
    = link_to icon_edit, edit_income_path(income.id), class: 'dark' unless income.is_nulled?
