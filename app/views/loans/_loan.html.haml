%li.row-fluid
  .span5
    %h4.ib
      = link_to loan, loan_path(loan.id)
    = loan.loan_type_tag
    = link_to loan.contact, loan.contact, title: 'Contacto'
    .clearfix
    = lo loan.date
    %strong(title='Estado')= loan.state_tag
  .span3
    %span.muted Intereses
    = ntc loan.interests
    = currency_label loan.currency
    .clearfix
    = loan.creator_label
    = loan.updater_label
    = loan.due_date_tag
    .clearfix
    %i.icon-file.muted(title='Descripción')
    = loan.description
  .span3
    .span6.r
      = ntc loan.total
      = currency_label loan.currency
    .span6.r
      = ntc loan.amount
      = currency_label loan.currency
