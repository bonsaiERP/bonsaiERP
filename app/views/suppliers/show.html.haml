- content_for :links do
  %ul.breadcrumps
    %li #{link_to "Clientes", clients_path} <span>&gt;</span>
  .links
    = link_to 'Nuevo', new_client_path, :class => 'new'
    = link_to 'Editar', edit_client_path(@supplier), :class => 'edit'
    = link_to 'Borrar', @supplier, :class => 'delete'

%h1= @supplier.matchcode

.clear
= render 'contacts/show', :klass => @supplier

- @currency_rates = CurrencyRate.current_hash

.clear
- @supplier.accounts.each do |ac|
  .moneybox.fl
    %label Cuenta en #{ac.currency_symbol}
    %h3= ntc ac.amount


.clear

= jquery_tabs :tab_id => "contact_tab" do
  =jqueryui_ul do
    =tab "Transacciones", client_path(@supplier, :tab => 'transactions'), "transactions"
    =tab "Compras", client_path(@supplier, :tab => 'buys'),"buys"
    =tab "Ventas", client_path(@supplier, :tab => 'incomes'),"incomes"
    =tab "Gastos", client_path(@supplier, :tab => 'expenses'),"expenses"
    
  = render @partial, @locals


:javascript
  $(function() {
  });

