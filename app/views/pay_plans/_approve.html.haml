%h2 Aprobar plan de créditos

- url = transaction.is_a?(Income) ? approve_credit_income_path(transaction) : approve_credit_buy_path(transaction)
= simple_form_for(transaction, :remote => true, :url => url) do |f|
  = f.input :credit_reference, :label => 'Referencia'
  .clear
  = f.input :credit_description, :label => 'Desripción', :as => :text,
    :input_html => {:rows => 5}
  .clear
  = f.submit "Aprobar crédito"

.clear
